<?xml version="1.0" encoding="UTF-8"?>
<dialects>
    <dialect type="POLYPHENY">
        <procedure name="NewOrder">
            <statement name="InsertNewOrder">
                INSERT INTO new_order (no_o_id, no_d_id, no_w_id) VALUES (?, ?, ?)
            </statement>
        </procedure>
        <procedure name="Payment">
            <statement name="UpdateCustomerBalance">
                UPDATE customer SET c_balance = c_balance - ? WHERE c_id = ?
            </statement>
        </procedure>
        <procedure name="StockLevel">
            <statement name="SelectStock">
                SELECT * FROM stock WHERE s_w_id = ?
            </statement>
        </procedure>
    </dialect>
</dialects>
